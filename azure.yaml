name: galaxy-chat-hub
metadata:
  template: galaxy-chat-hub-v0.0.1
services:
  backend:
    project: .
    language: js
    host: containerapp
    docker:
      path: Dockerfile.backend
      context: .
  frontend:
    project: .
    language: ts
    host: containerapp
    docker:
      path: Dockerfile.frontend
      context: .
